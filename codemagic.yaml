workflows:
  release-build:
    name: Debug Build (for store)
    environment:
      flutter: stable
    scripts:
      - flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk

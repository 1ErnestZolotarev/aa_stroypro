workflows:
  release-build:
    name: Debug Build with Verbose Log
    environment:
      flutter: stable
    scripts:
      - flutter build apk --debug --verbose 2>&1 | tee build-verbose.log
    artifacts:
      - build-verbose.log
      - build/app/outputs/flutter-apk/app-debug.apk
